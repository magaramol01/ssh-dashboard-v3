# Implementation Plans

Generated by the improve skill on 2026-09-17. Execute in order.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Build a custom NOAA-backed voyage optimizer | P1 | L | — | TODO |

## Dependency notes

- Plan 001 intentionally does not call the VPM API or copy the VPM Python optimizer services. It builds the route/weather path in this Nuxt project using the public NOAA/NWS API.
- NOAA/NWS coverage is not global open-ocean coverage. Routes or legs outside available NWS grid coverage must be marked as uncovered; they must not receive fabricated weather values.

## Findings considered and rejected

- Reusing the VPM optimizer service scripts: rejected by requirement; the scripts are reference material only.
- Rebuilding the segmented control shown in the reference image: rejected because the control already exists in `app/components/voyage/VoyageOptimizationHeaderBar.vue:105-149`; the work is wiring it to real custom route results.
- Treating NWS station observations as route forecasts: rejected because stations are coastal/land observations and cannot provide continuous open-ocean route weather.
